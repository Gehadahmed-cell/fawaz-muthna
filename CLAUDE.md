# قرارات المشروع

## البنية
- موقع Jekyll على GitHub Pages. لا يوجد package.json ولا خطوة بناء محلية.
- ممنوع إضافة أي تبعية npm أو إنشاء package.json.
- الشيفرة المشتركة (هيدر، فوتر، فورم التواصل، أيقونات) في `_layouts/default.html`.
- صفحات الخدمات تُولَّد من `_data/services.yml` — لا تُحرَّر يدوياً كملفات منفصلة.

## المحتوى
- ممنوع اختراع أي رقم: إحصائيات، عدد عملاء، سنوات، نسب مئوية. إذا احتاج التصميم
  رقماً ولا يوجد مصدر موثوق، استخدم صياغة وصفية بلا أرقام.
- بطاقات "من نحن" وصفية بلا أرقام (خبرة ممتدة / تغطية واسعة / فريق متخصص / التزام بالمواعيد).
- لا رابط فيسبوك — الصفحة غير موجودة.
- النصوص القائمة تُنقل حرفياً. ممنوع إعادة الصياغة أو الترجمة.

## التصميم
- عربي RTL: `dir="rtl"` و `lang="ar"`، والتخطيط والأيقونات تتبع الاتجاه.
- لا توجد صور عالية الدقة. لا تبنِ تخطيطات تعتمد على صور كبيرة، ولا تكبّر أي صورة
  فوق دقتها الأصلية. الاعتماد على التنضيد والمساحات البيضاء.
- CSS معياري بمتغيرات مخصصة. لا Tailwind ولا أي إطار.

## الجودة
- HTML5 دلالي، `h1` واحد لكل صفحة.
- Mobile-first، بلا تمرير أفقي عند 320px.
- تباين WCAG AA، وحلقة تركيز ظاهرة لكل عنصر تفاعلي.
- `width`/`height` على كل صورة.

## Git
- ممنوع الدفع. المستخدم يراجع ويدفع بنفسه.
- الفرع البعيد المعتمد: `claude/website-design-update-8jxmay`.
